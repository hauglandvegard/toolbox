[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "toolbox"
version = "0.1.0"
description = "My personal python toolbox."
authors = [{ name = "Vegard"}]
dependencies = [

]
